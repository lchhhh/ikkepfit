@model ikeepfit.Models.mt_planmark

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>mt_planmark</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.mt_planmark_id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.mt_planmark_id)
            @Html.ValidationMessageFor(model => model.mt_planmark_id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.mt_plan_id, "mt_plan")
        </div>
        <div class="editor-field">
            @Html.DropDownList("mt_plan_id", String.Empty)
            @Html.ValidationMessageFor(model => model.mt_plan_id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.mt_mark_id, "mt_mark")
        </div>
        <div class="editor-field">
            @Html.DropDownList("mt_mark_id", String.Empty)
            @Html.ValidationMessageFor(model => model.mt_mark_id)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
